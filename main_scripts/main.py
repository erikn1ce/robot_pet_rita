from main_scripts import config, recognize_face, recognize_speech, chase_laser, synthesis_speech, serial, get_capture
import threading
import asyncio


if __name__ == '__main__':
    print(config.names)
